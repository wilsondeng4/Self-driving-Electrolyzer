# Self-driving CO2 electrolyzer for ethylene-selective electrolyte 
Authors: Haoyang Deng, Hyeon Seok Lee, Mahyar Rajabi Kochi, *et al.*

## Directory descriptions

### SDE hardware code

This folder contains all the SDE hardware code

`SDEMain.py`:

`NewEraPump.py`:

`NeweriPump.py`:

`ni_controller.py`:

`psd6_pumps_25mL.py`:

`sensirion_mfc.py`:

`serial_device.py`:

`spec_sensor.py`:

### SDE hardware code/sequence

This folder contains all the sequences used in the `SDEMain.py`

`activation_sequences.csv`:

`calibration_sequences.csv`:

`repeatability_sequences.csv`:

`training_dataset_sequences.csv`:

`validation_sequences.csv`:


### Result data

`SDE raw data.xlsx`: 

`sample_space.csv`:

## Bayesian Optimization resource used


BOTorch library here
